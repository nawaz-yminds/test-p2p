{
  "version": 2,
  "builds": [
    { "src": "client/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "ignoreCommand": "git diff --quiet HEAD -- || echo changed",
  "cleanUrls": true
}
